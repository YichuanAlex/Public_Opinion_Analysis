# Python dependency for writing amplification .xlsx workbooks.
openpyxl>=3.1.2

# macOS OCR adapter. Windows uses the bundled WeChatOCR directly and must not
# fall back to these packages.
pyobjc-framework-Vision>=10.0; sys_platform == "darwin"
pyobjc-framework-Quartz>=10.0; sys_platform == "darwin"

# Local speech-to-text for downloaded video/audio media.
faster-whisper>=1.1.0
huggingface-hub>=0.23.0

# Visualization external PDF parsing.
pypdf>=4.3.1

# Comment_Data/comment_analysis_requirements.txt
pandas>=2.0.0
openpyxl>=3.1.0
requests>=2.31.0
jieba>=0.42.1
wordcloud>=1.9.3
matplotlib>=3.7.0
pillow>=10.0.0
